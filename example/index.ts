import simple from '../dist/index'
// @ts-ignore
import simpleMin from '../dist/simple.min.js'

console.log(simple, simpleMin)
document.body.innerHTML = 'simple: ' + JSON.stringify(simple) + '</br>' +
  'simpleMin: ' + JSON.stringify(simpleMin)
document.body.innerHTML = 'simple: ' + JSON.stringify(simple) + '</br>' +
  'simpleMin: ' + JSON.stringify(simpleMin)